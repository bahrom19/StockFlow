{
  "name": "stockflow-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "StockFlow Flutter Web end-to-end authentication suite (Playwright)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
