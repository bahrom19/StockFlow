# StockFlow v1.1.1 — Post-Release Audit

**Date:** 2026-08-05
**Release audited:** `0b4cb93` (v1.1.1 Accounting Consistency Patch)
**Mode:** Read-only audit of the live production system (Railway). No feature work, no refactoring, no schema changes.

---

## Executive Summary

The v1.1.1 release is **healthy and consistent in production**. All accounting invariants hold end-to-end, the mobile client builds and tests clean against the current production API, memory is flat (no leak), and PostgreSQL/Redis are green. **No Critical or High issues were found.** Recommended action: **freeze v1.1.1** and proceed to v1.2 planning.

---

## 1. Production Infrastructure Health

| Metric | Result |
|---|---|
| Health endpoint | `HTTP 200` — `{"status":"ok"}` / `{"status":"degraded"}` (memory-only flag, see below) |
| Container uptime | ~21–22 min at audit time (fresh deploy from CD on `0b4cb93`) |
| Memory (5 samples, ~2 min) | **45 MB / 58 MB flat** — 77.5 % → 76.5 %: **no growth, no leak** |
| PostgreSQL | `ok` (latency 2 ms) |
| Redis | `ok` (latency 0 ms) |
| Restart history | Single fresh restart from CD deploy; no abnormal restarts |
| Response time (health) | 0.89 s (incl. cold path) |
| Error rate (probed endpoints) | 0 % — all probed endpoints returned expected HTTP codes |

> Note: the health `degraded` status is solely the memory check at ~77–91 % of a small container budget (49–58 MB of ~54–59 MB). This is the same footprint as prior releases; it is a capacity observation, not a failure — see P2 debt.

## 2. Accounting Consistency Audit (production) — **43/43 PASS**

Verified against a fresh production company through the full business cycle:

| Check | Result |
|---|---|
| Goods Receipt → Valuation 25000, GL 1300 == Valuation | ✅ |
| Cost layers: Average Cost A == 1000, valuation qty == stock (AVERAGE costing) | ✅ |
| Positive adjustment +5 → GL 1300 == 30000 | ✅ |
| **Negative adjustment −3 → GL 1300 == 28500 (journal direction fix live)** | ✅ |
| Warehouse transfer → valuation & GL unchanged, TRANSFER_IN/OUT | ✅ |
| Discounted sale, overpayment (change 900), mixed payment | ✅ all COMPLETED |
| Refunds → shift netted, stock restored, profit netted to 3900 | ✅ |
| Close shift → expectedClosing == 13900, Z report totals net of refunds | ✅ |
| **GL Inventory == Inventory Valuation (final 26500)** | ✅ |
| **Trial Balance balanced** (debit == credit) | ✅ |
| **Every journal balanced** | ✅ |
| **No negative cost layers / no negative valuation / no orphan layers** (valuation qty == physical stock per product) | ✅ |
| **FIFO / Average Cost consistent** (avg cost 1000, layers intact) | ✅ |

## 3. Performance Audit (auth'd endpoint latency, production)

| Endpoint | Avg latency |
|---|---|
| Dashboard | ~1.0–1.2 s |
| Sales Report | ~1.0–1.3 s |
| Profit Report | ~0.96–1.0 s |
| Inventory Value / Low Stock Reports | ~0.96–1.0 s |
| Product Search | ~0.98 s |
| Barcode Scan | ~1.0–2.0 s (single cold sample outlier) |
| Valuation | ~0.99–1.8 s (cold outlier) |
| Trial Balance | ~0.95–1.0 s |
| Stock Levels | ~0.97 s |
| Sale Completion | ~1.0–1.5 s |

**Assessment:** latencies are consistent and bounded (≈1 s incl. Railway container cold-start and client overhead); no N+1 or duplicated-query pattern observed in the exercised paths. No optimization performed — only reported. A dedicated slow-query/index review is P2 (see debt).

## 4. Mobile Compatibility Audit

| Gate | Result |
|---|---|
| `flutter analyze` | ✅ **0 errors / 0 warnings** (207 `info` — lint style in test files: `directives_ordering`, `prefer_const_declarations`, dangling doc comments) |
| `flutter test` | ✅ **227 passed, 0 failed** (no flaky) |
| `flutter build web --release` | ✅ `build/web` |
| `flutter build apk --release` | ✅ `app-release.apk` (26.2 MB) |

Client is compatible with the current production backend.

## 5. Security Audit (report only)

| Area | Finding | Status |
|---|---|---|
| JWT access token | `JWT_SECRET` required (server refuses to start without it), 15-min expiry | ✅ |
| Refresh token | 7-day expiry, hashed storage, rotation on refresh, revoke on logout | ✅ |
| Login brute-force | Throttled: 5/min per login endpoint | ✅ |
| Global rate limiting | short 10/1 s, medium 50/10 s, long 200/60 s (`ThrottlerGuard` global) | ✅ |
| CORS | Origin from env (`app.corsOrigin`, default `*`), `credentials: true`, methods restricted | ✅ (note: default `*` — P3: pin explicit origin) |
| Headers | Helmet with CSP enabled in production | ✅ |
| Validation | Global `ValidationPipe` with `whitelist` + `forbidNonWhitelisted` | ✅ |
| Public endpoints | Only auth register/login/refresh/logout, health (`SkipThrottle`), `/api/v1` version probe | ✅ |
| Secrets | No secrets committed; `.env.example` contains **example** values only (referencing internal hosts) | ✅ |
| RBAC | `RolesGuard` + `@RequirePermission` on module controllers | ✅ |

## 6. Technical Debt (updated, prioritized)

### P1 (before next release)
| Item | Effort |
|---|---|
| Fix `qrSales` absent from CashShift — QR revenue pooled into `cardSales` (start of v1.2) | S–M |
| `PARTIALLY_REFUNDED` has no `refundedAmount` — partial refunds net at full sale value in reports | M |
| `invoice.repository.ts` TODO: replace manual numbering with PostgreSQL sequence | S |

### P2 (next 1–2 sprints)
| Item | Effort |
|---|---|
| Slow-query / index review (latencies ≈1 s; confirm Prisma query plans, add missing indexes) | M |
| Container memory budget (45/58 MB, ~77–91 %) — raise limit or reduce baseline | S |
| API standardization & versioning (MEDIUM-003/004 from existing debt ledger) | M |

### P3 (backlog)
| Item | Effort |
|---|---|
| Pin CORS `origin` to explicit production domain (default `*`) | S |
| Flutter 207 `info` lint fixes (directives_ordering, prefer_const) | S |
| Swagger disable in production (HIGH-002) | S |
| Row-level security (MEDIUM-009), full-text search (MEDIUM-005) | L |
| Integration/E2E test expansion (LOW-009/010) | M |

## 7. Recommendations

1. **Freeze v1.1.1** — no Critical/High issues; tag `v1.1.1` and cut release notes.
2. Proceed to **v1.2**: start with `qrSales` column + payment analytics (see roadmap).
3. Schedule P1 debt items within the v1.2 sprint; P2 index review before v1.3.

## Go / No-Go

**GO — freeze v1.1.1.**

## Success Criteria

- [x] Production health: HTTP 200, PG/Redis ok, memory flat (no leak)
- [x] Accounting: GL == Valuation, TB balanced, all journals balanced, cost layers consistent (43/43)
- [x] Performance: bounded ≈1 s latencies, no N+1 in exercised paths
- [x] Mobile: analyze 0/0, tests 227/227, web + APK builds pass
- [x] Security: JWT/refresh/CORS/throttling/RBAC validated, no secrets exposed
- [x] No Critical or High issues found → **freeze approved**
