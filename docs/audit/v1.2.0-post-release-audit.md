# StockFlow v1.2.0 Phase 1 — Post-Release Cross-Module Audit

**Date:** 2026-08-05
**Release:** `v1.2.0-phase1` (commit `85dd176`) — Payment Analytics Foundation (per-method cash-shift allocation: CASH / CARD / QR / BANK_TRANSFER / MOBILE_WALLET)
**Environment:** Production Railway (`stockflow-production-04c7.up.railway.app`) — fresh company created for the audit, full business cycle, real DB writes, real journals.
**Method:** Read-only. No code changed, no commits, no fixes. All invariants verified against the live production API.

---

## Executive Summary

| | |
|---|---|
| **Critical issues** | **0** |
| **High issues** | **0** |
| **Medium / Low** | 0 new (2 pre-existing, non-blocking — see Findings) |
| **Accounting consistency** | ✅ Trial Balance balanced, journals balanced, GL == Valuation |
| **Payment analytics** | ✅ all 5 methods allocated + refunded exactly; invariant holds |
| **Reports consistency** | ✅ Dashboard == Profit == Sales Report == Cash Shift |
| **Inventory consistency** | ✅ GR/adjust/transfer/sale/refund all reconcile; valuation == quantities × avg cost |
| **Performance** | ✅ unchanged (~1s endpoints, no regressions) |
| **Production stability** | ✅ memory flat, no restarts, PG/Redis healthy |
| **Recommendation** | **GO** ✅ |

Independent live verification: **61/61 PASS** (fresh production company, full cycle).

---

## 1. Accounting Audit — ✅ PASS

| Check | Result |
|---|---|
| Trial Balance balanced | ✅ `totalDebit == totalCredit` (> 0) after full cycle (GR → adjust ± → transfer → 4 sales → refund → close) |
| Journal Entries balanced | ✅ every posted entry balanced (no unbalanced-entry errors across 5 sale/refund scenarios) |
| GL Inventory == Valuation | ✅ inventory account postings track valuation at every step (GL drives trial balance; valuation verified independently) |
| Refunds reverse correctly | ✅ refund restores full inventory quantity and nets the cash shift exactly |
| Payment analytics affect accounting? | ❌ none — accounting behavior identical to v1.1.1 (GL/journal/costing/FIFO/avg-cost untouched) |

## 2. Payment Analytics Audit — ✅ PASS (all 5 methods)

| Scenario | Result |
|---|---|
| Exact payment (CASH 1000) | ✅ |
| Discounted sale (CARD 400 = 500 − 100) | ✅ |
| Overpayment (900 total / 1800 cash, change 900) | ✅ cash netted to 900 in shift + report |
| Mixed payment — **all 5 methods** (CASH 1000 + CARD 500 + QR 400 + BANK_TRANSFER 300 + MOBILE_WALLET 200) | ✅ each bucket exact |
| Refund (mixed S4) | ✅ exact reversal: cash −1000, card −500, qr −400, bank −300, wallet −200 |
| Multiple refunds path (full-refund scenario verified; partial-refund stays in revenue scope per P1 design) | ✅ |

**Invariant verified live on the Cash Shift:**
- Shift after 4 sales: cash 2900 + card 900 + qr 400 + bank 300 + wallet 200 = **4700 == totalSales**
- After refund: cash 1900 + card 400 + qr 0 + bank 0 + wallet 0 = **2300 == totalSales** ✅

## 3. Reports Cross-Check — ✅ PASS

| Report | Value | Match |
|---|---|---|
| Profit Report revenue | 2300 | ✅ |
| Sales Report revenue | 2300 | ✅ |
| Dashboard grossRevenue | 2300 | ✅ |
| Cash Shift totalSales | 2300 | ✅ |
| X Report totalSales | 2300 | ✅ |
| Sales Report payments sum (cash 1900 + card 400) | 2300 | ✅ `== revenue` |

All reports mutually consistent after refunds.

## 4. Inventory Audit — ✅ PASS

| Step | Stock A (WH1/WH2) | Stock B | Valuation |
|---|---|---|---|
| After Goods Receipt (20 + 20 @ 500/250) | 20 | 20 | 15000 |
| After adjust +5 (A), −3 (B) | 25 | 17 | 16750 |
| After transfer 4 A → WH2 | 21 / 4 | 17 | 16750 (unchanged) |
| After sales (S1 −1A, S2 −1B, S3 −1A, S4 −2A) | 17 / 4 | 16 | — |
| After refund S4 (+2A, full qty) | **19 / 4** | **16** | **15500** |

- Cost layers / average cost: A 500, B 250 held throughout (AVERAGE costing intact).
- Movements recorded: GR / ADJUST_IN / ADJUST_OUT / TRANSFER_IN / TRANSFER_OUT / SALE / REFUND ✅
- Valuation == physical quantity × average cost at every step ✅

## 5. API Compatibility — ✅ PASS

- CashShift response adds `qrSales` / `bankTransferSales` / `mobileWalletSales` — **additive only**; `cashSales` / `cardSales` / `totalSales` / `expectedClosing` / `difference` / `rowVersion` unchanged.
- Sales Report `summary.payments` adds `bankTransfer` / `mobileWallet`, retains legacy `cash` / `card` / `qr` / `other`.
- Old Flutter clients tolerant (lenient `_str()` parsing + freezed ignores unknown keys) — verified in code + live shape check.
- No breaking DTO changes; `MOBILE_WALLET` appended to enum lists without reordering.

## 6. Performance — ✅ no regressions

Measured live on production (fresh cold company, first-request latencies):

| Endpoint | Time |
|---|---|
| Register / Login | 2.1s / 1.5s (includes tenant provisioning + refresh token) |
| Dashboard | 1.0–1.6s |
| Sales Report / Profit / Cash-shift report | 0.9–1.1s |
| Trial Balance | 1.1s |
| Goods Receipt | 1.3–1.6s |
| Sale completion (×4) | 1.1–1.5s |
| Refund | 1.1s |
| Stock adjust / transfer / movements | 1.1–1.3s |
| Valuation | 1.0–1.3s |

Same ~1s envelope as the v1.1.1 post-release audit — no regression introduced by payment analytics. The allocation work is O(payments) in-memory; no N+1 or duplicated queries added.

## 7. Production Health / Memory — ✅ stable

| Metric | Sample 1 | Sample 2 | Sample 3 |
|---|---|---|---|
| Uptime | 722s | 737s | 753s |
| Memory | 49/59 MB (82.6%) | 48/58 MB (83.6%) | 49/58 MB (84.0%) |
| PostgreSQL | ok | ok | ok |
| Redis | ok | ok | ok |

- Memory **flat** across samples — no leak trend.
- Uptime ~12min at sampling = fresh CD deployment; **no abnormal restarts**.
- The container memory check hovers ~83% of a small 60MB budget — a capacity observation (P2, unchanged from v1.1.1), not a failure.

---

## Findings

| # | Severity | Finding | Status |
|---|---|---|---|
| 1 | — | None (accounting, reports, inventory, payments all consistent) | — |
| 2 | Medium (pre-existing) | Container memory budget small (~83% used) — Railway `memory` health check may flag `degraded` | Carry-forward (P2, from v1.1.1 audit) |
| 3 | Low (pre-existing) | Legacy `GIFT_CARD` / `STORE_CREDIT` pooled into `other` in shift buckets (unchanged v1.1.1 behavior, out of v1.2 Phase 1 scope) | Phase 2 backlog |

## Risks

- **Low:** none new. The only operational note is the small memory budget on the single-container deployment — recommend monitoring; if `degraded` persists at scale, raise the Railway memory limit (ops, not code).

## Recommendations

1. Keep v1.2 Phase 1 as-is — GO.
2. Phase 2: surface the per-method breakdown in the Flutter web dashboard / POS shift panel (API is ready and additive).
3. Phase 2: decide dedicated buckets vs `other` for GIFT_CARD / STORE_CREDIT.
4. Ops: bump the Railway container memory budget to remove the recurring `degraded` memory flag.

---

## GO / NO-GO

**GO** ✅

- 0 Critical, 0 High, 0 new Medium/Low.
- Accounting consistent (Trial Balance balanced, GL == Valuation, refunds exact).
- Reports consistent (Dashboard == Profit == Sales Report == Cash Shift == X).
- Inventory consistent (valuation == qty × avg cost, cost layers intact).
- Performance unchanged; production stable (memory flat, no restarts, PG/Redis healthy).
- Success criteria fully met.

`v1.2.0-phase1` is verified production-ready and may be frozen.
